import AccountType from "./components/AccountType"

export default function accountdPage() {
  return (
    <main className="flex items-center justify-center w-full min-h-[100vh]">
        <AccountType />
    </main>
  )
}
